{% extends 'admin/base.html.twig' %}

{% block title %}Tableau de Bord Financier{% endblock %}

{% block contentadmin %}
<div class="min-h-screen bg-green-50 from-slate-50 via-emerald-50/30 to-slate-100 font-poppins">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        
        {# Header #}
        <div class="mb-8">
            <h1 class="text-4xl font-black text-transparent bg-clip-text bg-gradient-to-r from-emerald-600 to-emerald-800 mb-2">
                💰 Tableau de Bord Financier
            </h1>
            <p class="text-slate-600 text-lg">Vue d'ensemble des revenus, frais et factures</p>
        </div>

        {# KPI Cards #}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            {# Revenus du mois #}
            <div class="bg-white rounded-3xl p-6 shadow-lg border-2 border-emerald-100 hover:shadow-xl transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <div class="w-12 h-12 bg-gradient-to-br from-emerald-500 to-emerald-600 rounded-2xl flex items-center justify-center">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                    </div>
                </div>
                <div class="text-3xl font-black text-slate-800 mb-1">{{ monthlyRevenue|number_format(0, ',', ' ') }} <span class="text-lg">FCFA</span></div>
                <div class="text-sm text-slate-500 font-semibold">Revenus du mois</div>
            </div>

            {# Frais collectés #}
            <div class="bg-white rounded-3xl p-6 shadow-lg border-2 border-green-100 hover:shadow-xl transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <div class="w-12 h-12 bg-gradient-to-br from-green-500 to-green-600 rounded-2xl flex items-center justify-center">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"/>
                        </svg>
                    </div>
                </div>
                <div class="text-3xl font-black text-slate-800 mb-1">{{ monthlyFees|number_format(0, ',', ' ') }} <span class="text-lg">FCFA</span></div>
                <div class="text-sm text-slate-500 font-semibold">Frais collectés</div>
            </div>

            {# Factures émises #}
            <div class="bg-white rounded-3xl p-6 shadow-lg border-2 border-green-100 hover:shadow-xl transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <div class="w-12 h-12 bg-gradient-to-br from-green-500 to-green-600 rounded-2xl flex items-center justify-center">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                        </svg>
                    </div>
                </div>
                <div class="text-3xl font-black text-slate-800 mb-1">{{ monthlyInvoices }}</div>
                <div class="text-sm text-slate-500 font-semibold">Factures émises</div>
            </div>

            {# Factures en attente #}
            <div class="bg-white rounded-3xl p-6 shadow-lg border-2 border-orange-100 hover:shadow-xl transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <div class="w-12 h-12 bg-gradient-to-br from-orange-500 to-orange-600 rounded-2xl flex items-center justify-center">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                    </div>
                </div>
                <div class="text-3xl font-black text-slate-800 mb-1">{{ pendingInvoices }}</div>
                <div class="text-sm text-slate-500 font-semibold">Factures en attente</div>
            </div>
        </div>

        {# Charts Row #}
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-8">
            {# Revenue Chart #}
            <div class="lg:col-span-2 bg-white rounded-3xl p-8 shadow-lg border-2 border-slate-100">
                <h2 class="text-2xl font-bold text-slate-800 mb-6">📈 Évolution des Revenus (12 mois)</h2>
                <canvas id="revenueChart" height="100"></canvas>
            </div>

            {# Fees by Type #}
            <div class="bg-white rounded-3xl p-8 shadow-lg border-2 border-slate-100">
                <h2 class="text-2xl font-bold text-slate-800 mb-6">🏷️ Frais par Type</h2>
                <div class="space-y-4">
                    {% for fee in feesByType %}
                        <div class="border-l-4 border-emerald-500 pl-4 py-2">
                            <div class="flex justify-between items-center">
                                <span class="font-semibold text-slate-700">{{ fee.type|replace({'_': ' '})|title }}</span>
                                <span class="text-emerald-600 font-bold">{{ fee.total|number_format(0, ',', ' ') }} FCFA</span>
                            </div>
                            <div class="text-sm text-slate-500">{{ fee.count }} transaction(s)</div>
                        </div>
                    {% else %}
                        <p class="text-slate-400 text-center py-8">Aucun frais ce mois</p>
                    {% endfor %}
                </div>
            </div>
        </div>

        {# Recent Transactions Table #}
        <div class="bg-white rounded-3xl p-8 shadow-lg border-2 border-slate-100">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-slate-800">🔄 Dernières Transactions</h2>
                <a href="{{ path('admin_transactions') }}" class="px-4 py-2 bg-emerald-500 text-white rounded-xl hover:bg-emerald-600 transition-colors font-semibold">
                    Voir tout
                </a>
            </div>
            
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead>
                        <tr class="border-b-2 border-slate-200">
                            <th class="text-left py-4 px-4 font-bold text-slate-700">ID</th>
                            <th class="text-left py-4 px-4 font-bold text-slate-700">Utilisateur</th>
                            <th class="text-left py-4 px-4 font-bold text-slate-700">Montant</th>
                            <th class="text-left py-4 px-4 font-bold text-slate-700">Méthode</th>
                            <th class="text-left py-4 px-4 font-bold text-slate-700">Date</th>
                            <th class="text-left py-4 px-4 font-bold text-slate-700">Statut</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for transaction in recentTransactions %}
                            <tr class="border-b border-slate-100 hover:bg-slate-50 transition-colors">
                                <td class="py-4 px-4 font-mono text-sm">#{{ transaction.id }}</td>
                                <td class="py-4 px-4">
                                    {% if transaction.utilisateur %}
                                        <div class="font-semibold text-slate-800">{{ transaction.utilisateur.firstname }} {{ transaction.utilisateur.lastname }}</div>
                                        <div class="text-sm text-slate-500">{{ transaction.utilisateur.email }}</div>
                                    {% else %}
                                        <span class="text-slate-400">N/A</span>
                                    {% endif %}
                                </td>
                                <td class="py-4 px-4 font-bold text-emerald-600">{{ transaction.amount|number_format(0, ',', ' ') }} FCFA</td>
                                <td class="py-4 px-4">
                                    <span class="px-3 py-1 bg-green-100 text-green-700 rounded-full text-sm font-semibold">
                                        {{ transaction.paymentMethod|default('N/A') }}
                                    </span>
                                </td>
                                <td class="py-4 px-4 text-slate-600">{{ transaction.createdAt|date('d/m/Y H:i') }}</td>
                                <td class="py-4 px-4">
                                    <span class="px-3 py-1 bg-green-100 text-green-700 rounded-full text-sm font-semibold">
                                        {{ transaction.statut }}
                                    </span>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="6" class="py-12 text-center text-slate-400">
                                    Aucune transaction récente
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</div>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700;900&display=swap');
    .font-poppins { font-family: 'Poppins', sans-serif; }
</style>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    const ctx = document.getElementById('revenueChart').getContext('2d');
    new Chart(ctx, {
        type: 'line',
        data: {
            labels: {{ revenueChart.labels|json_encode|raw }},
            datasets: [{
                label: 'Revenus (FCFA)',
                data: {{ revenueChart.data|json_encode|raw }},
                borderColor: '#10b981',
                backgroundColor: 'rgba(16, 185, 129, 0.1)',
                borderWidth: 3,
                fill: true,
                tension: 0.4,
                pointBackgroundColor: '#10b981',
                pointBorderColor: '#fff',
                pointBorderWidth: 2,
                pointRadius: 5,
                pointHoverRadius: 7
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: true,
            plugins: {
                legend: {
                    display: false
                },
                tooltip: {
                    backgroundColor: '#1e293b',
                    titleColor: '#fff',
                    bodyColor: '#fff',
                    padding: 12,
                    borderColor: '#10b981',
                    borderWidth: 2,
                    displayColors: false,
                    callbacks: {
                        label: function(context) {
                            return context.parsed.y.toLocaleString('fr-FR') + ' FCFA';
                        }
                    }
                }
            },
            scales: {
                y: {
                    beginAtZero: true,
                    ticks: {
                        callback: function(value) {
                            return value.toLocaleString('fr-FR') + ' FCFA';
                        }
                    },
                    grid: {
                        color: '#f1f5f9'
                    }
                },
                x: {
                    grid: {
                        display: false
                    }
                }
            }
        }
    });
</script>
{% endblock %}
