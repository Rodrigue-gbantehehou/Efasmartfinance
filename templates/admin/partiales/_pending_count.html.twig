{% if count > 0 %}
    <span class="inline-flex items-center justify-center px-2 py-0.5 ml-2 text-[10px] font-bold leading-none text-white bg-amber-500 rounded-full shadow-sm shadow-amber-200/50 animate-pulse">
        {{ count }}
    </span>
{% endif %}
