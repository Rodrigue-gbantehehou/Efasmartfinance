{% extends 'dashboard/base.html.twig' %}

{% block title %}2FA Activé - Codes de Secours{% endblock %}

{% block contentdashboard %}
<div class="max-w-4xl mx-auto px-4 py-12">
    <div class="bg-white rounded-[2.5rem] shadow-2xl shadow-slate-200/60 overflow-hidden border border-white animate-slide-up">
        <!-- Success Header -->
        <div class="bg-green-700 p-8 sm:p-12 text-center text-white relative overflow-hidden">
            <div class="absolute top-0 left-0 w-full h-full opacity-10">
                <i class="fas fa-shield-alt text-[15rem] absolute -top-10 -right-10 rotate-12 text-yellow-500"></i>
            </div>
            
            <div class="relative z-10">
                <div class="w-20 h-20 bg-yellow-500 backdrop-blur-md rounded-3xl flex items-center justify-center mx-auto mb-6 border border-yellow-400/30 shadow-xl">
                    <i class="fas fa-check text-3xl text-green-700"></i>
                </div>
                <h1 class="text-3xl sm:text-4xl font-black mb-2 tracking-tight">Félicitations !</h1>
                <p class="text-green-50 text-lg font-medium opacity-90">L'authentification à deux facteurs est maintenant active sur votre compte.</p>
            </div>
        </div>

        <div class="p-8 sm:p-12">
            <!-- Alert Box -->
            <div class="bg-yellow-50 rounded-3xl p-6 border border-yellow-100 flex items-start space-x-4 mb-10">
                <div class="w-12 h-12 bg-yellow-100 rounded-2xl flex items-center justify-center flex-shrink-0">
                    <i class="fas fa-exclamation-triangle text-green-700"></i>
                </div>
                <div>
                    <h3 class="text-slate-900 font-bold mb-1 text-lg">Action requise : Sauvegardez vos codes</h3>
                    <p class="text-slate-600 leading-relaxed font-medium">
                        Si vous perdez l'accès à votre boîte e-mail, ces codes seront le **seul moyen** de retrouver l'accès à votre compte. 
                        Stockez-les dans un endroit sûr (gestionnaire de mots de passe, coffre-fort numérique, ou impression physique).
                    </p>
                </div>
            </div>

            <!-- Codes Grid -->
            <div class="mb-10">
                <div class="flex items-center justify-between mb-6">
                    <h2 class="text-xl font-black text-slate-900 flex items-center">
                        <i class="fas fa-key text-green-700 mr-3"></i>
                        Vos 10 codes de secours
                    </h2>
                    <span class="px-4 py-1.5 bg-slate-100 text-slate-600 text-xs font-bold rounded-full uppercase tracking-wider">
                        Usage unique
                    </span>
                </div>

                <div class="grid grid-cols-2 sm:grid-cols-5 gap-4">
                    {% for code in backupCodes %}
                    <div class="group relative">
                        <div class="bg-slate-50 border-2 border-slate-100 rounded-2xl p-4 text-center font-mono font-black text-lg text-slate-800 shadow-sm transition-all hover:border-yellow-500 hover:bg-white hover:shadow-md hover:-translate-y-1">
                            {{ code }}
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>

            <!-- Actions -->
            <div class="flex flex-col sm:flex-row gap-4 pt-6 border-t border-slate-100">
                <button onclick="downloadCodes()" class="flex-1 bg-slate-900 hover:bg-black text-white font-bold py-4 px-6 rounded-2xl transition-all shadow-xl shadow-slate-200 flex items-center justify-center group">
                    <i class="fas fa-download mr-3 group-hover:bounce"></i>
                    Télécharger (.txt)
                </button>
                <button onclick="window.print()" class="flex-1 bg-white border-2 border-slate-200 text-slate-700 font-bold py-4 px-6 rounded-2xl hover:bg-slate-50 transition-all flex items-center justify-center">
                    <i class="fas fa-print mr-3"></i>
                    Imprimer
                </button>
                <a href="{{ path('app_dashboard_2fa_index') }}" class="flex-1 bg-green-700 hover:bg-yellow-500 hover:text-green-900 text-white font-bold py-4 px-6 rounded-2xl transition-all shadow-lg shadow-green-900/10 flex items-center justify-center">
                    Terminer
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    @keyframes slideUp {
        from { opacity: 0; transform: translateY(40px); }
        to { opacity: 1; transform: translateY(0); }
    }
    .animate-slide-up {
        animation: slideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    }
    @media print {
        header, sidebar, nav, footer, button, .no-print {
            display: none !important;
        }
        body { background: white !important; }
        .animate-slide-up { animation: none !important; transform: none !important; }
        .rounded-\[2\.5rem\] { border-radius: 0 !important; }
        .shadow-2xl { shadow: none !important; }
    }
</style>

<script>
function downloadCodes() {
    const codes = [{% for code in backupCodes %}"{{ code }}"{% if not loop.last %}, {% endif %}{% endfor %}];
    const content = "CODES DE SECOURS EFA SMART FINANCE\nGénérés le : " + new Date().toLocaleString() + "\n\n" + 
                    "Ces codes sont à usage unique. Gardez-les précieusement.\n\n" + 
                    codes.map((c, i) => `Code ${i+1}: ${c}`).join("\n");
    
    const blob = new Blob([content], { type: 'text/plain' });
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = 'efa-backup-codes.txt';
    link.click();
    URL.revokeObjectURL(url);
}
</script>
{% endblock %}
