{% extends 'base.html.twig' %}

{% block title %}Email Vérifié - EFA Smart Finance{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 text-center">
                <div class="card">
                    <div class="card-body p-5">
                        <div class="mb-4">
                            <i class="fas fa-check-circle text-success" style="font-size: 5rem;"></i>
                        </div>
                        <h1 class="h3 mb-4">Félicitations !</h1>
                        <p class="lead">Votre adresse email a été vérifiée avec succès.</p>
                        <p>Vous pouvez maintenant profiter de toutes les fonctionnalités de votre compte EFA Smart Finance.</p>
                        <div class="mt-4">
                            <a href="{{ path('app_home') }}" class="btn btn-primary">
                                <i class="fas fa-home me-2"></i>Retour à l'accueil
                            </a>
                            <a href="{{ path('app_dashboard') }}" class="btn btn-outline-primary ms-2">
                                <i class="fas fa-tachometer-alt me-2"></i>Tableau de bord
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
