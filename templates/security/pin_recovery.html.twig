{% extends 'base.html.twig' %}

{% block title %}Récupération du Code PIN - EFA Smart Finance{% endblock %}

{% block body %}
<div class="min-h-screen bg-green-50 flex items-center justify-center p-4 py-12">
    <div class="max-w-md w-full bg-white rounded-2xl shadow-xl overflow-hidden border border-gray-100 scale-in-center">
        <!-- Header -->
        <div class="bg-gradient-to-r from-emerald-600 to-teal-600 p-8 text-center">
            <div
                class="inline-flex items-center justify-center w-16 h-16 bg-white/10 rounded-full mb-4 backdrop-blur-sm">
                <i class="fas fa-user-shield text-3xl text-white"></i>
            </div>
            <h1 class="text-2xl font-bold text-white mb-2">Récupération Sécurisée</h1>
            <p class="text-emerald-50 text-sm">Réinitialisation de votre code PIN après verrouillage</p>
        </div>

        <div class="p-8">
            {% for label, messages in app.flashes %}
            {% for message in messages %}
            <div
                class="mb-6 p-4 rounded-xl {% if label == 'success' %}bg-emerald-50 text-emerald-700 border border-emerald-200{% else %}bg-rose-50 text-rose-700 border border-rose-200{% endif %} flex items-center gap-3 animate-pulse">
                <i class="fas {% if label == 'success' %}fa-check-circle{% else %}fa-exclamation-circle{% endif %}"></i>
                <span class="text-sm font-medium">{{ message }}</span>
            </div>
            {% endfor %}
            {% endfor %}

            <div class="bg-amber-50 border-l-4 border-amber-400 p-4 mb-8 rounded-r-lg">
                <div class="flex items-start">
                    <div class="flex-shrink-0">
                        <i class="fas fa-info-circle text-amber-500 mt-0.5"></i>
                    </div>
                    <div class="ml-3">
                        <p class="text-xs text-amber-800 leading-relaxed">
                            Pour réinitialiser votre code PIN, vous devez fournir votre Identifiant Unique (UUID) et
                            votre mot de passe de compte.
                        </p>
                    </div>
                </div>
            </div>

            <form method="POST" class="space-y-6">
                <!-- UUID Input -->
                <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2 uppercase tracking-wider">Identifiant
                        Unique (UUID)</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-gray-600">
                            <i class="fas fa-id-card"></i>
                        </div>
                        <input type="text" name="uuid" required
                            class="block w-full pl-11 pr-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 transition-all text-sm"
                            placeholder="Ex: 8830846">
                    </div>
                </div>

                <!-- Password Input -->
                <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2 uppercase tracking-wider">Mot de passe
                        du compte</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-gray-600">
                            <i class="fas fa-lock"></i>
                        </div>
                        <input type="password" name="password" required
                            class="block w-full pl-11 pr-12 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 transition-all text-sm"
                            placeholder="••••••••">
                    </div>
                </div>

                <div class="pt-4 border-t border-gray-100">
                    <h3 class="text-xs font-bold text-gray-600 uppercase mb-4 tracking-widest">Nouveau Code PIN</h3>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-semibold text-gray-600 mb-2 italic">Nouveau PIN</label>
                            <input type="password" name="new_pin" required maxlength="5" pattern="\d{5}"
                                class="block w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 transition-all text-center text-xl font-mono tracking-[0.5em]"
                                placeholder="00000">
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-gray-600 mb-2 italic">Confirmation</label>
                            <input type="password" name="confirm_pin" required maxlength="5" pattern="\d{5}"
                                class="block w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 transition-all text-center text-xl font-mono tracking-[0.5em]"
                                placeholder="00000">
                        </div>
                    </div>
                    <p class="mt-2 text-[10px] text-gray-600 italic">Le code PIN doit contenir exactement 5 chiffres.
                    </p>
                </div>

                <button type="submit"
                    class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-bold py-4 px-6 rounded-xl shadow-lg shadow-emerald-200 transform active:scale-95 transition-all flex items-center justify-center gap-3">
                    <i class="fas fa-sync-alt"></i>
                    Réinitialiser le PIN
                </button>
            </form>

            <div class="mt-8 pt-6 border-t border-gray-100 text-center">
                <p class="text-sm text-gray-600 mb-4">Besoin d'aide supplémentaire ?</p>
                <div class="flex flex-col gap-3">
                    <a href="mailto:contact@binajia.org"
                        class="text-emerald-600 hover:text-emerald-700 font-semibold text-sm flex items-center justify-center gap-2">
                        <i class="fas fa-envelope"></i> Contacter le support technique
                    </a>
                    <a href="{{ path('app_login') }}"
                        class="text-gray-600 hover:text-gray-600 text-sm flex items-center justify-center gap-2">
                        <i class="fas fa-arrow-left"></i> Retour à la connexion
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .scale-in-center {
        animation: scale-in-center 0.5s cubic-bezier(0.250, 0.460, 0.450, 0.940) both;
    }

    @keyframes scale-in-center {
        0% {
            transform: scale(0);
            opacity: 1;
        }

        100% {
            transform: scale(1);
            opacity: 1;
        }
    }
</style>
{% endblock %}