<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Efa Smart Finance{% endblock %}</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: #F0F9F1;
            margin: 0;
            padding: 0;
            -webkit-font-smoothing: antialiased;
        }

        .wrapper {
            width: 100%;
            table-layout: fixed;
            background-color: #F0F9F1;
            padding-bottom: 40px;
        }

        .main {
            
            margin: 0 auto;
            width: 100%;
            max-width: 600px;
            border-spacing: 0;
            color: #1e293b;
            border-radius: 16px;
            overflow: hidden;
            margin-top: 20px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
        }

        .header {
            padding: 30px;
            text-align: center;
        }

        .content {
            padding: 40px 30px;
            line-height: 1.6;
            font-size: 16px;
            background-color: #ffffff;
        }

        .footer {
            background-color: #006400;
            padding: 30px;
            text-align: center;
            font-size: 12px;
            color: #ffffff;
        }

        .button {
            display: inline-block;
            padding: 14px 28px;
            background-color: #006400;
            color: #ffffff !important;
            text-decoration: none;
            border-radius: 12px;
            font-weight: bold;
            margin-top: 20px;
        }

        .brand-green {
            color: #006400;
        }

        .brand-gold {
            color: #f59e0b;
        }

        h1 {
            font-size: 24px;
            color: #0f172a;
            margin-top: 0;
        }

        p {
            margin: 16px 0;
        }

        .social-link {
            margin: 0 10px;
            text-decoration: none;
            color: #94a3b8;
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <table class="main" width="100%">
            <tr>
                <td class="header">
                    <img src="cid:logo-efa.png" alt="Efa Smart Finance" width="160"
                        style="outline: none; border: none;">
                </td>
            </tr>
            <tr>
                <td class="content">
                    {% block content %}{% endblock %}
                </td>
            </tr>
            <tr>
                <td class="footer">

                    <p style="margin: 0;">&copy; {{ "now"|date("Y") }} Efa Smart Finance. Tous droits réservés.</p>
                    <p style="margin: 5px 0 0 0; font-size: 11px;">La révolution de la tontine digitale.</p>
                </td>
            </tr>
        </table>
    </div>
</body>

</html>