<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Efa Smart Finance — Maintenance en cours</title>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:wght@400;500;600&display=swap"
        rel="stylesheet">

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        'sans': ['Inter', 'sans-serif'],
                        'jakarta': ['Plus Jakarta Sans', 'sans-serif'],
                    },
                    colors: {
                        'brand': {
                            50: '#F0F9F1',
                            100: '#DDEFE0',
                            600: '#006400',
                            700: '#004d00',
                        },
                        'gold': '#f59e0b',
                    }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #F0F9F1;
            background-image:
                radial-gradient(at 0% 0%, rgba(0, 100, 0, 0.05) 0px, transparent 50%),
                radial-gradient(at 100% 0%, rgba(245, 158, 11, 0.05) 0px, transparent 50%);
        }

        .glass {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-15px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .float-anim {
            animation: float 4s ease-in-out infinite;
        }

        .shimmer {
            background: linear-gradient(90deg, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.4) 50%, rgba(255, 255, 255, 0) 100%);
            background-size: 200% 100%;
            animation: shimmer 2.5s infinite;
        }

        @keyframes shimmer {
            0% {
                background-position: -200% 0;
            }

            100% {
                background-position: 200% 0;
            }
        }
    </style>
</head>

<body class="min-h-screen flex items-center justify-center p-6 font-sans">

    <div class="max-w-xl w-full text-center space-y-8 animate-fade-in">

        <!-- Logo -->
        <div class="flex justify-center mb-12">
            <div class="relative">
                <div class="absolute inset-0 bg-brand-600 blur-2xl opacity-10 rounded-full scale-150"></div>
                <!-- Vous pouvez remplacer par le chemin de votre logo réel -->
                <div class="relative bg-white p-4 rounded-3xl shadow-xl border border-brand-100">
                    <img src="/images/logovert.png" alt="Efa Logo" class="h-16 w-auto"
                        onerror="this.innerHTML='<i class=\'fas fa-shield-halved text-brand-600 text-4xl\'></i>'; this.type='hidden';">
                </div>
            </div>
        </div>

        <!-- Illustration / Icon -->
        <div class="relative py-10">
            <div class="float-anim inline-block">
                <div class="relative h-40 w-40 flex items-center justify-center">
                    <!-- Rotating rings -->
                    <div
                        class="absolute inset-0 border-4 border-dashed border-brand-100 rounded-full animate-[spin_20s_linear_infinite]">
                    </div>
                    <div
                        class="absolute inset-4 border-2 border-brand-600/20 rounded-full animate-[spin_15s_linear_infinite_reverse]">
                    </div>

                    <div
                        class="h-28 w-28 bg-brand-600 text-white rounded-3xl flex items-center justify-center shadow-2xl shadow-brand-600/30">
                        <i class="fas fa-tools text-5xl"></i>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <div class="space-y-4">
            <h1 class="text-4xl md:text-5xl font-jakarta font-extrabold text-slate-900 tracking-tight">
                Amélioration en cours
            </h1>
            <p class="text-lg text-slate-500 max-w-md mx-auto leading-relaxed">
                {{ message|raw }}
            </p>
        </div>

        <!-- Info Cards -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-12">
            <div class="glass p-5 rounded-3xl shadow-sm text-left flex gap-4 items-center">
                <div class="h-10 w-10 shrink-0 bg-brand-50 text-brand-600 rounded-2xl flex items-center justify-center">
                    <i class="fas fa-clock text-lg"></i>
                </div>
                <div>
                    <div class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">Démarrage</div>
                    <div class="text-sm font-bold text-slate-900">{{ startedAt ? startedAt|date('d/m/Y à H:i') : "En
                        cours" }}</div>
                </div>
            </div>

            <div class="glass p-5 rounded-3xl shadow-sm text-left flex gap-4 items-center">
                <div class="h-10 w-10 shrink-0 bg-amber-50 text-amber-600 rounded-2xl flex items-center justify-center">
                    <i class="fas fa-bolt text-lg"></i>
                </div>
                <div>
                    <div class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">Temps estimé</div>
                    <div class="text-sm font-bold text-slate-900">Quelques minutes</div>
                </div>
            </div>
        </div>

        <!-- Progress Bar (Fake) -->
        <div class="w-full h-1.5 bg-brand-100 rounded-full overflow-hidden mt-8">
            <div class="h-full bg-brand-600 shimmer w-3/4 rounded-full"></div>
        </div>

        <!-- Footer Contact -->
        <div class="pt-12 space-y-4">
            <p class="text-xs font-bold text-slate-400 uppercase tracking-[0.2em]">Besoin d'assistance ?</p>
            <div class="flex flex-wrap justify-center gap-6">
                <a href="mailto:support@efasmartfinance.com"
                    class="text-slate-600 hover:text-brand-600 transition-colors flex items-center gap-2 font-medium">
                    <i class="fas fa-envelope text-brand-600"></i>
                    support@efasmartfinance.com
                </a>
            </div>
            <p class="text-[10px] text-slate-400 font-medium">
                © {{ "now"|date('Y') }} Efa Smart Finance. La révolution de la tontine digitale.
            </p>
        </div>

    </div>

</body>

</html>